# SafeHour Frontend

React + Vite + Leaflet MVP.

Set `VITE_API_URL` if the backend is not running at `http://localhost:8000`.

For Netlify, configure the build command:

```text
npm run build
```

and publish directory:

```text
dist
```

Set the base directory to `frontend`.
